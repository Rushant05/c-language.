#include<stdio.h>
main()
{
	int number;
	printf("enter the number");
	scanf("%d",&number);
	
	if(number>0)
	{
		printf("number is positive");
	}
		if(number<0)
	{
		printf("number is nagative");
	}
		if(number==0)
	{
		printf("number is nutural");
	}
}
